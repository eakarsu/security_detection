server {
  listen 3000;
  server_name _;

  location = /api/v1/config {
    proxy_pass http://nodejs-api:3001/api/v1/config;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
}
